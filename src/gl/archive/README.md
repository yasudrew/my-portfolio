# archive

Scenes from earlier directions, kept because the work is reusable rather than
because it is wired up. Nothing here is imported by the app.

- `fluid/` — a full Stable Fluids solver (advection → vorticity → pressure
  projection → gradient subtract) on ping-pong FBOs. Candidate for a Playground
  entry.
- `env/` — a studio environment map built in-process with PMREM, no external
  HDR fetch.
- `HeroFluid.tsx`, `HeroObject.tsx` — the hero scenes those two supported.
